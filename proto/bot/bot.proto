syntax = "proto3";
package bot;

service Application {
    rpc Start (StartRequest) returns (StartResponse);
}

message StartRequest {
    string bot_id = 1;
}

message StartResponse {
    bool success = 1;
}
