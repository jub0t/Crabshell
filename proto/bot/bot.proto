syntax = "proto3";
package bot;

service Bot {
    rpc StartBot (StartRequest) returns (StartResponse);
}

message StartRequest {
    string bot_id = 1;
}

message StartResponse {
    bool success = 1;
}
